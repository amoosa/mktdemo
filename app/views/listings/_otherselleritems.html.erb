<h4>Other items from this seller...</h4>

<div class="center">
  <div class="row">
    <% Listing.where("user_id = ? AND id != ?", @listing.user, @listing.id).order("random()").limit(6).each do |listing| %>
        <div class="col-md-2 col-sm-3 col-xs-6">
        <div class="thumbnail">
          <%= link_to image_tag(listing.image.url(:medium), class: "img-responsive"), listing, data: { no_turbolink: true } %>
          <div class="caption">
            <h3><%= link_to listing.name.downcase.titleize, listing, data: { no_turbolink: true } %></h3>
            <p><%= number_to_currency(listing.price) %></p>
          </div>
        </div>
     </div>
    <% end %>
  </div>
</div>
<br>