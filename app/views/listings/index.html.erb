<% if params[:page].nil? %>
<div class="jumbotron">
  <h1>test <br>
      banner.</h1>
  <h2>shop now.</h2>
</div>

<%= render 'featured' %>
<% end %>

<h4>New Additions</h4>

<!--   <div class="dropdown">
    <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown">
            Sort<span class="caret"></span>
    </button>
    <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
      <li><%= link_to "Price", :sort => "price" %></li>
      <li><%= link_to "Most Recent", :sort => "created_at" %></li>
      <li><%= link_to "Alphabetical", :sort => "name" %></li>
    </ul>
  </div>
  <br> -->


<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
      <div class="col-md-3 col-sm-3 col-xs-6 images">
        <div class="thumbnail">
          <%= link_to image_tag(listing.image.url(:medium), class: "img-responsive"), listing, data: { no_turbolink: true } %>
          <div class="caption">
            <h3><%= link_to listing.name.downcase.titleize, listing, data: { no_turbolink: true } %></h3>
            <p><%= number_to_currency(listing.price) %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <p><%= will_paginate @listings, renderer: BootstrapPagination::Rails %></p>
</div>




