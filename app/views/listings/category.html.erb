<h4>Shop <font color="#428bca"><%= @category %></font> </h4>

 <div class="dropdown">
    <button class="btn btn-default btn-xs dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown">
            Sort <%= params[:sort] %> <b class="caret"></b> </button>

    <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
      <li><%= link_to "New Arrivals", category_path(:sort => "- New Arrivals")  %></li>
      <li><%= link_to "Price - Low to High", category_path(:sort => "- Price - Low to High") %></li>
      <li><%= link_to "Price - High to Low", category_path(:sort => "- Price - High to Low")  %></li>
      <li><%= link_to "Discover - Random Shuffle", category_path(:sort => "- Random Shuffle")  %></li>
    </ul>

</div>
  <br>

<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
        <div class="col-md-2 col-sm-3 col-xs-6 images">
        <div class="thumbnail">
          <%= link_to image_tag(listing.image.url(:medium), class: "img-responsive"), listing, data: { no_turbolink: true } %>
          <div class="caption">
            <h3><%= link_to listing.name.downcase.titleize, listing, data: { no_turbolink: true } %></h3>
            <p><%= number_to_currency(listing.price) %></p>
          </div>
        </div>
     </div>
    <% end %>
  </div>
  <p><%= will_paginate @listings, renderer: BootstrapPagination::Rails %></p>
</div>
<br>
