
<header role="banner" class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <div class="navbar-header">
          <button data-toggle="collapse-side" data-target=".side-collapse" data-target-2=".side-collapse-container" type="button" class="navbar-toggle pull-left"><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button>
          <a class="navbar-brand" href="/">outfit additions</a>
        </div>
        <div class="navbar-inverse side-collapse in">
          <nav role="navigation" class="navbar-collapse">
            <!-- CATEGORY DROP DOWN -->
       <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Category<b class="caret"></b></a>
           <ul class="dropdown-menu">
             <li><%- Listing.uniq.pluck(:category).each do |category| %>
               <%= link_to category, category_path(category: category) %> 
              <% end %>
             </li>
           </ul>
         </li>
        </ul>
   <!-- END CATEGORY DROP DOWN -->

    <!-- ACCOUNT LINKS -->
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %><b class="caret"></b></a>
          <ul class="dropdown-menu">
            <% if current_user.name == "admin admin" %>
              <li><%= link_to "Admin", admin_path, data: { no_turbolink: true }  %></li>
            <% end %>
            <li><%= link_to "Your Purchases", purchases_path  %></li>
            <li><%= link_to "List Items for Sale", new_listing_path, data: { no_turbolink: true }  %></li>
            <li><%= link_to "Manage Listings", seller_path  %></li>
            <li><%= link_to "Sold Listings", sales_path  %></li>
            <li><%= link_to "Profile Info", edit_user_registration_path  %></li>
          </ul>
          </li>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete  %></li>
          <% else %>
            <li><%= link_to "Sign Up", new_user_registration_path  %></li>
            <li><%= link_to "Login", new_user_session_path  %></li>
        <% end %>
      </ul>
  <!-- END ACCOUNT LINKS -->

  <!-- SEARCH BOX -->
          <%= form_tag(search_path, :method => "get", id: "search-form", class:"navbar-form navbar-right") do %>
            <%= text_field_tag :search, params[:search], placeholder: "Search by color or style...", :size => "30", class:"form-control" %>
            <button type="submit" class="btn btn-default btn-md"><span class="glyphicon glyphicon-search"></span></button>
              <% end %>
  <!-- END SEARCH BOX --> 

          </nav>
        </div>
      </div>
    </header>